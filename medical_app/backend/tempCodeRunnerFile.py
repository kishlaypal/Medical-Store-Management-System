cursor = cnx.cursor()
# query="SELECT * FROM medicalstore.products;"
# cursor.execute(query)

# for (product_id,name,uom_id,dose,price_per_unit) in cursor:
#     print(product_id,name,uom_id,dose,price_per_unit)